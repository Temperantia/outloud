// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "inclusive.com"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
